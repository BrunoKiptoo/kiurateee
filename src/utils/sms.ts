// utils/smsService.ts
export const sendSms = async (mobileNumber: string, message: string) => {
  // Integration with SMS gateway service
  // Example using Twilio or any other SMS provider
  console.log(`Sending SMS to ${mobileNumber}: ${message}`);
};
